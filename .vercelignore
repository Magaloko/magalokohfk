# Secrets & lokale Daten NIEMALS deployen
config/
data/
backups/
audit/
.claude/
scripts/
# Lokaler Long-Poll-Server/Bot werden auf Vercel nicht gebraucht
server.mjs
telegram-bot.mjs
ecosystem.config.cjs
# Hinweis: v2/ NICHT hier ignorieren — Vercel liest diese Root-.vercelignore auch
# fürs V2-Projekt (Root Directory = v2/) und würde sonst v2/ entfernen (kein next gefunden).
# Das Live-Projekt baut aus dem Root (kein next in der Root-package.json) und ignoriert v2/ ohnehin.
# Große lokale Exporte (liegen ohnehin im Parent)
*.csv
*.log
