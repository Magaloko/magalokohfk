# === SECRETS — niemals committen, niemals zu externen Tools hochladen ===
config/auth.json
config/telegram.json
config/sessions.json

# === Laufzeit-Daten (Geschäftsdaten + Kunden-bezogen) — komplett raus ===
data/
# alle Config-JSONs außer dem Secret-freien Beispiel
config/*.json
!config/telegram.example.json

# === Deploy / Tooling ===
.vercel/
.claude/
audit/
backups/

# === Große Importe / generierte Inhalte (nicht Teil des Code-Audits) ===
lernsystem-2026/
marktanalyse-2026/

# === System ===
node_modules/
*.log
.DS_Store
Thumbs.db
qa-*.png
qa-*.html
